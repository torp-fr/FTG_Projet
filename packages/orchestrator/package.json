{
  "name": "@ftg/orchestrator",
  "version": "0.1.0",
  "private": true,
  "description": "Orchestrateur FTG : Sequencer, Router, Diffuser, Gatekeeper, Supervisor (Chantier 4 §4).",
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "tsx --test test/*.test.ts"
  },
  "dependencies": {
    "@ftg/database": "workspace:*",
    "@ftg/engine-sdk": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "@types/node": "^22.0.0"
  }
}
